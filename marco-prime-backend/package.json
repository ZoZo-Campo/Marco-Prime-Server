{
  "name": "backend",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "db:pull": "drizzle-kit pull",
    "db:push": "drizzle-kit push",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "db:seed": "tsx src/db/seed.ts",
    "catalog:sync": "tsx -e \"import('./src/services/fouaille-sync.service.ts').then(({ synchronizeFouailleCatalog }) => synchronizeFouailleCatalog()).then(console.log)\""
  },
  "dependencies": {
    "@hono/node-server": "^1.19.17",
    "@hono/swagger-ui": "^0.5.3",
    "@hono/zod-validator": "^0.7.6",
    "dotenv": "^17.2.3",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.13.7",
    "hono-rate-limiter": "^0.5.3",
    "mysql2": "^3.24.3",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@types/node": "^20.11.17",
    "@types/pg": "^8.16.0",
    "@vitest/coverage-v8": "4.0.17",
    "drizzle-kit": "^0.31.8",
    "drizzle-seed": "^0.3.1",
    "tsx": "^4.7.1",
    "typescript": "^5.8.3",
    "vitest": "^4.0.17"
  }
}
